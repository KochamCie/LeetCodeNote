package com.hama.leetcode;
/**
Given many words, words[i] has weight i.

Design a class WordFilter that supports one function, WordFilter.f(String prefix, String suffix). It will return the word with given prefix and suffix with maximum weight. If no word exists, return -1.

Examples:


Input:
WordFilter([apple])
WordFilter.f(a, e) // returns 0
WordFilter.f(b, ) // returns -1


&nbsp;

Note:


	words has length in range [1, 15000].
	For each test case, up to words.length queries WordFilter.f may be made.
	words[i] has length in range [1, 10].
	prefix, suffix have lengths in range [0, 10].
	words[i] and prefix, suffix queries consist of lowercase letters only.


&nbsp;
**/
class _746_Prefix_and_Suffix_Search {

    public _746_Prefix_and_Suffix_Search(String[] words) {
        
    }
    
    public int f(String prefix, String suffix) {
        
    }
}

/**
 * Your _746_Prefix_and_Suffix_Search object will be instantiated and called as such:
 * _746_Prefix_and_Suffix_Search obj = new _746_Prefix_and_Suffix_Search(words);
 * int param_1 = obj.f(prefix,suffix);
 */