package com.hama.leetcode;
/**
Design a data structure that supports the following two operations:


void addWord(word)
bool search(word)


search(word) can search a literal word or a regular expression string containing only letters a-z or .. A . means it can represent any one letter.

Example:


addWord(bad)
addWord(dad)
addWord(mad)
search(pad) -&gt; false
search(bad) -&gt; true
search(.ad) -&gt; true
search(b..) -&gt; true


Note:
You may assume that all words are consist of lowercase letters a-z.
**/
class _211_Add_and_Search_Word___Data_structure_design {

    /** Initialize your data structure here. */
    public _211_Add_and_Search_Word___Data_structure_design() {
        
    }
    
    /** Adds a word into the data structure. */
    public void addWord(String word) {
        
    }
    
    /** Returns if the word is in the data structure. A word could contain the dot character '.' to represent any one letter. */
    public boolean search(String word) {
        
    }
}

/**
 * Your _211_Add_and_Search_Word___Data_structure_design object will be instantiated and called as such:
 * _211_Add_and_Search_Word___Data_structure_design obj = new _211_Add_and_Search_Word___Data_structure_design();
 * obj.addWord(word);
 * boolean param_2 = obj.search(word);
 */