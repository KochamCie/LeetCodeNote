package com.hama.leetcode;
/**
Implement a trie with insert, search, and startsWith methods.

Example:


Trie trie = new Trie();

trie.insert(apple);
trie.search(apple);   // returns true
trie.search(app);     // returns false
trie.startsWith(app); // returns true
trie.insert(app);   
trie.search(app);     // returns true


Note:


	You may assume that all inputs are consist of lowercase letters a-z.
	All inputs are guaranteed to be non-empty strings.

**/
class _208_Implement_Trie__Prefix_Tree_ {

    /** Initialize your data structure here. */
    public _208_Implement_Trie__Prefix_Tree_() {
        
    }
    
    /** Inserts a word into the trie. */
    public void insert(String word) {
        
    }
    
    /** Returns if the word is in the trie. */
    public boolean search(String word) {
        
    }
    
    /** Returns if there is any word in the trie that starts with the given prefix. */
    public boolean startsWith(String prefix) {
        
    }
}

/**
 * Your _208_Implement_Trie__Prefix_Tree_ object will be instantiated and called as such:
 * _208_Implement_Trie__Prefix_Tree_ obj = new _208_Implement_Trie__Prefix_Tree_();
 * obj.insert(word);
 * boolean param_2 = obj.search(word);
 * boolean param_3 = obj.startsWith(prefix);
 */